<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 이 파일의 경로를 config.xml에 명시해야 한다. -->

<!-- Member 테이블에 대한 CRUD 기능을 구현하기 위한 Mapper -->
<mapper namespace="FavoriteMapper">
	
	<!-- Beans 클래스의 객체이름(id)과 클래스 이름(type)를 명시한다. -->
	<resultMap type="study.jsp.bookstory.model.Favorite" id="favorite">
	<!-- 빈즈의 멤버 변수(property) 이름과 대상테이블의 컬럼(column)을 연결한다-->
		<result property="id" column="id"/>
		<result property="member_id" column="member_id"/>
		<result property="book_id" column="book_id"/>
		<result property="gender" column="gender"/>
		<result property="birthdate" column="birthdate"/>		
	</resultMap>


	<insert id="insertAddEpisode" parameterType="study.jsp.bookstory.model.Favorite" useGeneratedKeys="true" keyProperty="id">
	INSERT INTO favorite (
	 member_id, book_id, gender, birthdate
										)
	VALUES (
	 #{member_id}, #{book_id}, gender, birthdate
	 )
	</insert> 
	
	
	<delete id="deleteRemoveEpisode" parameterType="study.jsp.bookstory.model.Favorite">
	 
	 DELETE FROM favorite WHERE id=#{id};	
	</delete>
	

</mapper>